language: python
python:
  - "3.7"
sudo: false
# command to install dependencies
install:
  - pip install .[test]
# command to run tests
script:
  - make test code-style code-lint
after_success:
  - coveralls
